{% extends 'ingredientBack.html.twig' %}

{% block title %}Ingredient{% endblock %}

{% block body %}
    <h1>Ingredient</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>ID</th>
                <td>{{ ingredient.id }}</td>
            </tr>
            <tr>
                <th>Categorie</th>
                <td>{{ ingredient.category }}</td>
            </tr>
            <tr>
                <th>Calories</th>
                <td>{{ ingredient.caloriesCategory }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ ingredient.name }}</td>
            </tr>
        </tbody>
    </table>


    {{ include('ingredient/_delete_form.html.twig') }}

    <br>
    <br>
    <a href="{{ path('ingredient_edit', {'id': ingredient.id}) }}"style="  background-color: #FFC300; color: white; padding: 10px 30px;  text-align: center;  text-decoration: none; display: inline-block;">Modifier</a>
    <br>
    <a href="{{ path('ingredient_index') }}"style="  background-color: #f44336; color: white; padding: 10px 35px;  text-align: center;  text-decoration: none; display: inline-block;">Retour</a>
{% endblock %}
